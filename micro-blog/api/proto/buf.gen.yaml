version: v1
managed:
  enabled: false
plugins:
  # generate go struct code
  - name: go
    out: ../gen/go
    opt: paths=source_relative

  # generate grpc service code
  - name: go-grpc
    out: ../gen/go
    opt: paths=source_relative

  # generate rest service code
  - name: go-http
    out: ../gen/go
    opt: paths=source_relative

  # generate micro errors code
  - name: go-errors
    out: ../gen/go
    opt: paths=source_relative

  # generate message validator code
  - name: validate
    out: ../gen/go
    opt:
      - paths=source_relative
      - lang=go
